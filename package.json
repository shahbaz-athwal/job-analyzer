{
  "name": "job-analyzer",
  "private": true,
  "workspaces": {
    "packages": [
      "apps/*",
      "packages/*"
    ],
    "catalog": {
      "dotenv": "^17.2.2",
      "zod": "^4.1.13",
      "typescript": "^5",
      "convex": "^1.31.2"
    }
  },
  "type": "module",
  "scripts": {
    "dev": "bun run --filter '*' dev",
    "build": "bun run --filter '*' build",
    "check-types": "bun run --filter '*' check-types",
    "dev:native": "bun run --filter native dev",
    "dev:web": "bun run --filter web dev",
    "dev:server": "bun run --filter @job-analyzer/backend dev",
    "dev:setup": "bun run --filter @job-analyzer/backend dev:setup"
  },
  "dependencies": {
    "@job-analyzer/env": "workspace:*",
    "dotenv": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.11",
    "@job-analyzer/config": "workspace:*",
    "typescript": "catalog:",
    "ultracite": "7.0.12"
  },
  "packageManager": "bun@1.3.3"
}
